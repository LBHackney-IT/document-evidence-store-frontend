export enum DocumentSubmissionTabState {
  ALL_DOCUMENTS = 'all-documents',
  AWAITING_SUBMISSION = 'awaiting-submission',
  PENDING_REVIEW = 'pending-review',
  APPROVED = 'approved',
  REJECTED = 'rejected',
}
